import express from "express";
//import {list, add, insert} from "../controllers/main_controller.js";
//import {validateUser} from "../middlewares/main_middleware.js";

let router = express.Router();
//localhost:3000/users/
//router.get("/", list);
//router.get("/add", add);
//router.post("/add", validatePrize,insert);
export default router;